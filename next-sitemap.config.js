/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://projectspce.com',
  generateRobotsTxt: true, // (optional)
  generateIndexSitemap: false,
  // ...other options
}

module.exports = config